#!/usr/bin/env bash

set -e

npm run build

cd build/

echo www.aboqasem.dev >CNAME

git init

git add -A

git commit -m 'Deploy'

git push -f git@github.com:aboqasem/portfolio-client.git main:gh-pages

cd -

rm -rf build/
